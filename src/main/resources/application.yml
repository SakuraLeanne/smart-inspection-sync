spring:
  datasource:
    sqlserver:
      driver-class-name: com.microsoft.sqlserver.jdbc.SQLServerDriver
      jdbc-url: jdbc:sqlserver://10.30.1.11:1433;databaseName=pms_xfzy;encrypt=false
      username: ${SQLSERVER_USERNAME:sa}
      password: ${SQLSERVER_PASSWORD:UwO3V3MDCM}
    mysql:
      driver-class-name: com.mysql.jdbc.Driver
      jdbc-url: jdbc:mysql://10.30.1.70:3306/abcadmin_default?useUnicode=true&characterEncoding=utf8&useSSL=false&serverTimezone=Asia/Shanghai
      username: ${MYSQL_USERNAME:root}
      password: ${MYSQL_PASSWORD:1qazxsw2.}

sync:
  batch-size:
    customer-service: 3000
    customer-service-history: 5000
    organization-item: 3000
    materials-inventory-request: 300
    materials-inventory-request-detail: 500
  throttle-sleep-ms:
    materials-inventory-request: 100
  sqlserver:
    use-row-number-pagination: false

# 全量同步接口：
# curl -X POST http://localhost:8080/api/sync/customer-service/full
# curl -X POST http://localhost:8080/api/sync/customer-service-history/full
# curl -X POST http://localhost:8080/api/sync/organization-item/full
# curl -X POST http://localhost:8080/api/sync/materials-inventory-request/full
# curl -X POST http://localhost:8080/api/sync/full

# 增量同步接口及方案：
# 增量同步仅同步源表 Id 大于上次成功水位的新增数据；已同步数据不回刷、不覆盖。
# CustomerService：Id 高水位。
# curl -X POST http://localhost:8080/api/sync/customer-service/incremental
# CustomerServiceHistory：Id 高水位。
# curl -X POST http://localhost:8080/api/sync/customer-service-history/incremental
# OrganizationItem：Id 高水位。
# curl -X POST http://localhost:8080/api/sync/organization-item/incremental
# MaterialsInventoryRequest/Detail：主表和明细各自 Id 高水位。
# curl -X POST http://localhost:8080/api/sync/materials-inventory-request/incremental
# curl -X POST http://localhost:8080/api/sync/incremental
